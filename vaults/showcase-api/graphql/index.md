---
title: GraphQL
description: One endpoint for reads that span pets, shelters, and adoptions.
---

Send every query and mutation as a `POST` to `https://api.harbour.example/v2/graphql` with the same bearer token as REST. [[authentication|Authentication]] covers tokens.
