# #17 MVP integration

The backend stack is validated through #70. This slice records final output evidence and removes stale status documents while the UI branch finishes separately.

- [x] Tests: the public-URL build verifies canonical HTML, sitemap routes, 404, icon assets, and runtime artifacts.
- [x] Capture Knowledge: N/A. This slice found no new constraint beyond the lazy-theme rule already captured in #70.
- [x] Documentation: replaced outdated `TODO.md`, `NEXT_STEPS.md`, and package contract statements with current v1 status.
- [x] Review & Close: checks passed and #72 is filed above #70. #16 is ready for review but its check remains pending; keep #17 open until the selected baseline reaches the default branch.
